<!doctype html>
{{> ieshim }}
  <head>
    {{> siteHead }}
    {{> favicons }}
    {{> osProtocol }}
  </head>
  <body>
  {{> browserCheck }}
  {{> navbar }}

    <div class="container">
      {{> breadcrumb }}
      {{> about }}


      <hr>
      {{> footer }}
    </div> {{! /container }}
    {{> siteJs }}

  </body>
</html>
